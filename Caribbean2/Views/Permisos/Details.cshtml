@model Caribbean2.Models.Permiso

@{
    ViewData["Title"] = "Detalles del Permiso";
    Layout = null;
}

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>@ViewData["Title"]</title>
    <link rel="stylesheet" href="@Url.Content("~/css/sideBar.css")">
    <link rel="stylesheet" href="@Url.Content("~/css/roles.css")">
    <link rel="stylesheet" href="/css/bootstrap.min.css">
    <link rel="stylesheet" href="@Url.Content("~/css/menuCentral.css")">
    <!-- Favicon -->
    <link href="img/favicon.ico" rel="icon">
    <!-- Google Fonts -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Heebo:wght@400;500;600&family=Nunito:wght@600;700;800&display=swap">
    <!-- Icon Font -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.4.1/font/bootstrap-icons.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
</head>

<body>
    <div class="container-fluid bg-dark px-5 d-lg-block d justify-content-around mb-2">
        <div class="row gx-0 p-2 align-items-center">
            <div class="btn-group-container d-flex justify-content-end">
                <a class="btn-modern d-flex d-lg-none" data-bs-toggle="tooltip" title="Cerrar sesión" href="@Url.Action("Logout", "Login")">
                    <i class="fas fa-sign-out-alt"></i>
                </a>
                <a href="@Url.Action("Logout", "Login")" class="btn-modern me-2 d-none d-lg-inline-flex">
                    <i class="fas fa-sign-out-alt"></i> Cerrar sesión
                </a>
            </div>
        </div>
    </div>

    <h1 class="d-flex justify-content-center mt-3">Gestión de Permisos</h1>
    <div class="container">
        <div class="row justify-content-center">
            <div class="col-12 col-md-6">
                <div class="roles-card">
                    <div class="roles-card-header d-flex justify-content-between align-items-center">
                        <div class="text-center flex-grow-1">
                            <h2 class="roles-card-title">Detalles del Permiso</h2>
                        </div>
                    </div>
                    <div class="roles-card-body p-3">
                        <dl class="row">
                            <dt class="col-sm-2">Permiso</dt>
                            <dd class="col-sm-10">@Html.DisplayFor(model => model.NombrePermiso)</dd>
                            <dt class="col-sm-2">Descripción</dt>
                            <dd class="col-sm-10">@Html.DisplayFor(model => model.DescripcionPermiso)</dd>
                        </dl>
                    </div>
                    <div class="text-center mt-4">
                        <a asp-action="Edit" asp-route-id="@Model?.IdPermiso ?? 0" class="btn btn-primary me-2">
                            <i class="bi bi-pencil-square"></i> Editar
                        </a>
                        <a asp-action="Index" class="btn btn-secondary">
                            <i class="bi bi-arrow-left-circle"></i> Regresar
                        </a>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <script src="@Url.Content("~/js/index.js")"></script>
    <script src="https://code.jquery.com/jquery-3.4.1.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
